{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.16.0/.schema/devbox.schema.json",
  "packages": [
    "python@3.14.0"
  ],
  "shell": {
    "init_hook": [
      "if [ -f .venv/bin/activate ]; then source .venv/bin/activate; else python -m venv .venv && source .venv/bin/activate; fi",
      "python -m ensurepip --upgrade",
      "python -m pip install --upgrade pip",
      "if [ -f requirements.txt ]; then python -m pip install -r requirements.txt && echo \"Installed requirements\"; fi"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ]
    }
  }
}